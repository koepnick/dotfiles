alias l='ls'
alias ll='ls -larht'
alias mkdir='mkdir -p'

alias vim='nvim'
alias dc='docker-compose'

alias ss_='neoss'

alias dc='docker-compose'

alias fget='wget --reject "*.exe" --reject "*.lnk" --reject "*.scr" --reject "*.zip" --mirror '
